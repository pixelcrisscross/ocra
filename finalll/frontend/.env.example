# URL of the ORCA Python backend (FastAPI).
# Local dev defaults to http://127.0.0.1:8000 when this is unset.
# For production (UI on Vercel, backend on a Python host) set this to the
# hosted backend URL, e.g. https://orca-backend.onrender.com
VITE_API_BASE_URL=https://your-backend-host.com
